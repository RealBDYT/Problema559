#include <iostream>
#include <math.h>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    short a, b, c, d, e,max1, max2, max3;

    cin>>a;
    cin>>b;
    cin>>c;
    cin>>d;
    cin>>e;

    //max1
    if (a >= b && a>= c && a >= d && a >= e) {
        max1 = a;
        a = 0;
    } else if (b >= c && b >= d && b >= e) {
        max1 = b;
        b = 0;
    } else if (c >= d && c >= e) {
        max1 = c;
        c = 0;
    } else if (d >= e) {
        max1 = d;
        d = 0;
    } else {
        max1 = e;
        e = 0;
    }

    //max 2
    if (a >= b && a>= c && a >= d && a >= e) {
        max2 = a;
        a = 0;
    } else if (b >= c && b >= d && b >= e) {
        max2 = b;
        b = 0;
    } else if (c >= d && c >= e) {
        max2 = c;
        c = 0;
    } else if (d >= e) {
        max2 = d;
        d = 0;
    } else {
        max2 = e;
        e = 0;
    }

    //max 3
    if (a >= b && a>= c && a >= d && a >= e) {
        max3 = a;
        a = 0;
    } else if (b >= c && b >= d && b >= e) {
        max3 = b;
        b = 0;
    } else if (c >= d && c >= e) {
        max3 = c;
        c = 0;
    } else if (d >= e) {
        max3 = d;
        d = 0;
    } else {
        max3 = e;
        e = 0;
    }

    cout<<max1 + max2 + max3<<endl;

    return 0;
}
